CREATE TABLE raksh_item_tab (
    item_id NUMBER GENERATED BY DEFAULT AS IDENTITY,
    availabl_qty NUMBER NOT NULL,
    scrapped_qty NUMBER NOT NULL,
    reserved_qty NUMBER NOT NULL,
    item_name VARCHAR2(200) NOT NULL,
    PRIMARY KEY (item_id)
);
